package com.example.a8_bitinvader;

import static org.junit.Assert.*;

import org.junit.Test;

/** Pop up window unit test. Comprehensive testing done in instrumented tests. */
public class PopUpWindowTest {

    /** No tests needed because methods are protected and not public */
    /** Default test for onCreate, no test needed as method is protected */
    @Test
    public void onCreate() {
        assertTrue(true);
    }
}